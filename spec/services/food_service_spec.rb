require 'rails_helper'

RSpec.describe FoodService do
  let(:a) { FoodService.b }

  it 'establishes a connection for top rated movies', :vcr do
    expect(x).to be_a()
    expect(x[:y]).to be_a()
    expect(x[:y].first[:z]).to be_a()
    expect(x[:y].first[:z]).to eq()
  end
end
